| item_id | Category | Item | Absent | Basic | Adequate | Advanced | Clarification |
|-|-|-|---|------|------|------|---|
| MD1 | Meta data | Starting point / README | Absent or unclear. | Present with some relevant items (see 'advanced') described or limited description. | Present with most relevant items (see 'advanced') described in detail. | Present including (or reference to) all relevant items described in detail:<br>• Model goal/purpose<br>• Developer(s) contact details<br>• Publication link/DOI detailing model scope (if applicable)<br>• Installation guidance (e.g., repository cloning such as from GitHub, required software and packages) | |
| MD2 | Meta data | Run guidance | Absent or unclear. | Present (in README file) with some relevant items (see 'advanced') described or limited description. | Present (in README file) with most relevant items (see 'advanced') described in detail. | Present (in README file) describing all relevant items described in detail:<br>• Starting file/code<br>• Inputs list/file<br>• Functions defined for the model and their workflow<br>• Generate outcomes<br>• Runtime (if longer than a minute) | |
| MD3 | Meta data | License | Absent or unclear. | — | — | Present using open license (e.g., MIT, Apache, BSD, GNU-GPL). | |
| MD4 | Meta data | Package & dependencies | Absent or unclear. | Coding language(s) and required package(s) are listed (in README file) without version details. | Coding language(s) and required package(s) are listed (in README file) including version details and dependencies in a description file. | Lockfile (snapshot of dependencies pinning versions) provided (e.g., distributed as R package or alternative for other coding languages). | |
| MD5 | Meta data | Model version (history) | Absent or unclear. | Model version is listed (in README file). | Model version and releases (and a description of the changes between versions) are described (in README file). | Versions and releases and their changes are documented (e.g., using code repository version and release functionality like GitHub, Zenodo or alternative with common practice version names such as pre-fix.major.minor.patch / v2.3.1). | |
| CQ1 | Coding quality | Folder structure | Absent or unclear (e.g., inputs, functions and output in single file or folder and/or human unreadable file names). | Inputs, functions and outputs are stored in separate files and folders with human readable file names. | (Common) template or framework folder structure specific for health-economic evaluation is used (e.g., DARTH coding framework) with human-readable file names. | Widely used folder structure applied (e.g., structure for R packages) with human-readable file names. | |
| CQ2 | Coding quality | Consistent style | Absent or unclear (e.g., unstructured coding style, uncommon abbreviations, inconsistent naming). | Less than half of the relevant elements of a common style guide are applied (see 'advanced'). | More than half of the relevant elements of a common style guide are applied (see 'advanced'). | All relevant elements of a common style guide (e.g., https://style.tidyverse.org/) are applied: <br>• Human readable object names<br>• Variable naming convention<br>• Variable type prefix (e.g., DARTH coding framework)<br>• Code spacing and indentation | |
| CQ3 | Coding quality | Coding documentation | Absent or unclear. | Inline headings and inline comments. | Inline headings and inline comments, and a description of the model flow. | Inline headings, inline comments, and vignettes or package documentation (e.g., R package 'roxygen2'). | |
| CQ4 | Coding quality | Functions / modules | Absent or unclear (e.g., single script with no functions). | The model is coded as a single function with preparing inputs and analyses mainly as single script. | The model is coded as only a few functions with distinct aims (e.g., preparing inputs and running analysis). | The model and analyses are fully modularized with clearly ordered functions for preparing inputs, run the model, run analyses and if applicable other parts (such as validation and calibration). | |
| RE1 | Reproducibility | Execution reproducibility | Model does not run (e.g., due to unclear starting point, failed installation of packages or errors). | Model runs but none of the model results are replicated. | The model runs but only the model's base case (typically from a publication results table) or example/synthetic input scenario results are replicated with reasonable deviations. | The model runs and all model results (typically from a publication results table) are replicated with no or reasonable deviations. | |
